(:name magit-gerrit
       :description "(Modified) Magit plugin for Gerrit Code Review."
       :type github
       :pkgname "zeph1e/magit-gerrit"
       :depends (magit)
       :post-init (eval-after-load 'magit
                    '(require 'magit-gerrit)))
